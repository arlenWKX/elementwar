\# 《元素战争：临界反应》整体架构



\---



\## 一、系统全景



```

┌──────────────────────────────────────────────────────────────┐

│                         玩家终端                              │

│  ┌─────────────┐                    ┌─────────────┐         │

│  │   浏览器     │◄──── WebSocket ───►│   浏览器     │         │

│  │  (Vue3 前端) │                    │  (Vue3 前端) │         │

│  └──────┬──────┘                    └──────┬──────┘         │

└─────────┼──────────────────────────────────┼────────────────┘

&#x20;         │                                  │

&#x20;         └──────────────┬───────────────────┘

&#x20;                        │

&#x20;                   ┌────┴────┐

&#x20;                   │  Nginx  │  ← 反向代理 + 静态文件

&#x20;                   └────┬────┘

&#x20;                        │

&#x20;       ┌────────────────┼────────────────┐

&#x20;       │                │                │

&#x20;       ▼                ▼                ▼

&#x20;  ┌─────────┐    ┌─────────────┐   ┌──────────┐

&#x20;  │ 静态资源 │    │  WebSocket  │   │ 管理接口  │

&#x20;  │ (Vue构建)│    │  (Socket.IO)│   │ (FastAPI) │

&#x20;  └─────────┘    └──────┬──────┘   └──────────┘

&#x20;                        │

&#x20;                        ▼

&#x20;             ┌─────────────────────┐

&#x20;             │    Python 游戏服     │

&#x20;             │  ┌───────────────┐  │

&#x20;             │  │  连接与房间层   │  │  ← 玩家接入、房间生命周期、断线重连

&#x20;             │  └───────┬───────┘  │

&#x20;             │          │           │

&#x20;             │  ┌───────┴───────┐  │

&#x20;             │  │   游戏逻辑层    │  │  ← 回合流转、行动判定、胜利条件

&#x20;             │  │  (状态机核心)   │  │

&#x20;             │  └───────┬───────┘  │

&#x20;             │          │           │

&#x20;             │  ┌───────┴───────┐  │

&#x20;             │  │   化学引擎层    │  │  ← 反应计算、配平（预留）

&#x20;             │  │  (内部模块调用) │  │

&#x20;             │  └───────┬───────┘  │

&#x20;             │          │           │

&#x20;             │  ┌───────┴───────┐  │

&#x20;             │  │   数据持久层    │  │  ← 反应缓存、纠错记录、连接状态

&#x20;             │  │    (SQLite)    │  │

&#x20;             │  └───────────────┘  │

&#x20;             └─────────────────────┘

&#x20;                        │

&#x20;                        ▼

&#x20;                   ┌─────────┐

&#x20;                   │ 宿主机卷 │  ← 配置文件、数据库文件持久化

&#x20;                   └─────────┘

```



\---



\## 二、分层边界



\### 第一层：前端表现层（Vue3 + CSS）



\*\*职责\*\*：渲染界面、响应用户输入、播放动画、维护本地 UI 状态。



\*\*不做的\*\*：

\- 不判定反应是否成立（只高亮"可能可打"的牌，最终有效性由服务端裁定）

\- 不维护权威游戏状态（只接收服务端同步的状态并渲染）

\- 不直接访问数据库或引擎



\*\*与后端契约\*\*：通过 WebSocket 收发事件，状态以服务端推送为准。



\---



\### 第二层：连接与房间层（Socket.IO）



\*\*职责\*\*：管理玩家连接、维护房间集合、处理匹配码、断线重连令牌发放。



\*\*边界\*\*：

\- 知道"哪个 socket 属于哪个玩家、哪个房间"

\- 不知道"这张牌能不能打、反应产物是什么"

\- 将原始事件转发给游戏逻辑层，并将逻辑层结果广播给房间内的玩家



\---



\### 第三层：游戏逻辑层（状态机核心）



\*\*职责\*\*：执行规则。回合开始/结束、行动顺序计算、卡牌流转、奖励分结算、胜利判定。



\*\*边界\*\*：

\- 持有权威的 `GameRoom` 对象，内含玩家牌区、场上物质、牌池等完整状态

\- 遇到需要化学知识的问题（"Na 和 H₂O 加热会怎样"），调用引擎层

\- 遇到需要持久化的问题（"这个反应之前算过吗"），查询数据层

\- 不直接操作网络连接（由房间层代为广播）



\---



\### 第四层：化学引擎层



\*\*职责\*\*：给定反应物、条件、温度、体积，返回是否反应、反应程度、产物及量。



\*\*边界\*\*：

\- 纯计算模块，无游戏概念（不知道"手牌""回合""玩家"）

\- 被游戏逻辑层以函数调用方式使用（同进程内）

\- 计算耗时操作异步隔离，不阻塞连接层



\---



\### 第五层：数据持久层（SQLite）



\*\*职责\*\*：反应结果缓存、纠错记录、断线重连令牌。



\*\*边界\*\*：

\- 被游戏逻辑层读写

\- 不暴露给前端

\- 同机文件存储，无网络端口



\---



\## 三、一次完整操作的跨层数据流



以"玩家打出一张钠，与水反应"为例：



```

\[前端] 玩家点击手牌中的"钠"，点击"接龙"按钮

&#x20;   │

&#x20;   ▼  WebSocket: action:react

\[连接层] 验证该 socket 对应玩家是否处于其轮次，转发事件

&#x20;   │

&#x20;   ▼

\[游戏逻辑层]

&#x20;   ├─ 检查玩家手牌中确有此牌

&#x20;   ├─ 检查当前场上物质为 H₂O

&#x20;   ├─ 调用引擎层：calculate(Na, H₂O, heat, 1L, 298K)

&#x20;   │

&#x20;   ▼  函数调用（同进程）

\[引擎层] 返回：valid=true, extent=complete, products=\[NaOH, H₂]

&#x20;   │

&#x20;   ▼

\[游戏逻辑层]

&#x20;   ├─ 更新场上物质为 NaOH

&#x20;   ├─ 将旧物质 H₂O 移入玩家弃牌堆

&#x20;   ├─ 检查是否触发连锁奖励（本次行动第几步？）

&#x20;   ├─ 检查是否满足胜利条件

&#x20;   └─ 构造状态变更描述

&#x20;   │

&#x20;   ▼

\[连接层] 向房间内所有玩家广播：

&#x20;   ├─ reaction:result（反应结果摘要）

&#x20;   ├─ state:sync（或增量更新，视实现而定）

&#x20;   │

&#x20;   ▼  WebSocket 推送

\[前端 ×2] 双方屏幕同步更新：

&#x20;   ├─ 场上物质变为 NaOH

&#x20;   ├─ 反应历史新增一条记录

&#x20;   ├─ 打出者手牌减少一张

&#x20;   └─ 若打出者牌库已空且手牌为0，触发胜利动画

```



\---



\## 四、部署视图



单服务器，单容器（或前后端分离两个容器）：



```

云服务器

├── Nginx（入口网关）

│   ├── / → 静态文件（Vue3 构建产物）

│   ├── /socket.io/ → 反向代理到 Python 游戏服 :3000

│   └── /api/ → 反向代理到 Python 游戏服 :3000（FastAPI 管理接口）

│

├── Python 游戏服容器（:3000）

│   ├── Socket.IO 服务器

│   ├── FastAPI 子应用

│   ├── 游戏逻辑 + 化学引擎（同进程）

│   └── aiosqlite 连接

│

└── 挂载卷 /app/data/

&#x20;   ├── materials.json      ← 物质牌配置（用户提供）

&#x20;   └── game.db             ← SQLite 数据库（自动生成）

```



\---



\## 五、关键设计决策



| 决策 | 选择 | 理由 |

|------|------|------|

| 前后端通信 | WebSocket (Socket.IO) | 实时回合制，需要服务端主动推送 |

| 服务端语言 | Python | 与化学引擎同语言，直接函数调用 |

| 进程模型 | 单进程合并（游戏服+引擎） | 2人卡牌并发极低，消除网络调用开销 |

| 引擎调用方式 | 异步隔离（线程池） | 防止复杂计算阻塞 WebSocket 事件循环 |

| 持久化 | SQLite 文件 | 同机部署，零额外依赖，缓存+纠错足够 |

| 前端框架 | Vue3 + 纯 CSS | 开发者最熟悉，移动端适配灵活，无需游戏引擎 |

| 对战模式 | 匹配码房间 | 无用户系统，极简接入 |

| AI 架构 | 服务端逻辑线程 | 前端无感知，状态同步与 PvP 完全一致 |

